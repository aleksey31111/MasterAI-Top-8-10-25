def todo_list_app():
    """
    Простое консольное приложение списка дел
    """

    # Список для хранения задач
    tasks = []

    print("=" * 40)
    print("        СПИСОК ДЕЛ (MVP ВЕРСИЯ)")
    print("=" * 40)

    # Главный цикл программы
    while True:
        # Меню
        print("\n" + "-" * 30)
        print("МЕНЮ:")
        print("1. Показать все задачи")
        print("2. Добавить задачу")
        print("3. Удалить задачу")
        print("4. Выйти")
        print("-" * 30)

        # Получение выбора пользователя
        choice = input("Выберите действие (1-4): ").strip()

        # 1. Показать все задачи
        if choice == "1":
            print("\n" + "=" * 30)
            print("СПИСОК ВАШИХ ЗАДАЧ:")
            print("=" * 30)

            if len(tasks) == 0:
                print("📭 Список задач пуст!")
            else:
                for i, task in enumerate(tasks, 1):
                    print(f"{i}. {task}")
            print(f"\nВсего задач: {len(tasks)}")

        # 2. Добавить задачу
        elif choice == "2":
            print("\n" + "-" * 30)
            print("ДОБАВЛЕНИЕ НОВОЙ ЗАДАЧИ")
            print("-" * 30)

            new_task = input("Введите описание задачи: ").strip()

            if new_task:  # Проверяем, что задача не пустая
                tasks.append(new_task)
                print(f"✅ Задача добавлена! Всего задач: {len(tasks)}")
            else:
                print("❌ Задача не может быть пустой!")

        # 3. Удалить задачу
        elif choice == "3":
            print("\n" + "-" * 30)
            print("УДАЛЕНИЕ ЗАДАЧИ")
            print("-" * 30)

            if len(tasks) == 0:
                print("📭 Список задач пуст! Нечего удалять.")
                continue

            # Показываем задачи с номерами
            print("Текущие задачи:")
            for i, task in enumerate(tasks, 1):
                print(f"{i}. {task}")

            try:
                # Запрашиваем номер задачи для удаления
                task_num = int(input(f"\nВведите номер задачи для удаления (1-{len(tasks)}): ").strip())

                if 1 <= task_num <= len(tasks):
                    removed_task = tasks.pop(task_num - 1)
                    print(f"✅ Задача удалена: '{removed_task}'")
                    print(f"📊 Осталось задач: {len(tasks)}")
                else:
                    print(f"❌ Неверный номер! Введите число от 1 до {len(tasks)}")

            except ValueError:
                print("❌ Ошибка! Пожалуйста, введите номер задачи.")

        # 4. Выйти из программы
        elif choice == "4":
            print("\n" + "=" * 40)
            print("Спасибо за использование списка дел!")
            print(f"Итоговое количество задач: {len(tasks)}")
            print("=" * 40)
            break

        # Неверный выбор
        else:
            print("❌ Неверный выбор! Пожалуйста, выберите 1, 2, 3 или 4.")


# Запуск приложения
if __name__ == "__main__":
    todo_list_app()